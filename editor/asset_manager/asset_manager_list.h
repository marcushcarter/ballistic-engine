#pragma once
#include <imgui.h>

namespace lumen {

struct AssetManagerList
{
    // void _list_item(ImTextureID p_icon, const char* p_main_text, const char* p_sub_text);
    // void draw(EditorContext& ctx, std::filesystem::path& selected, const char* search_buf);
};

}